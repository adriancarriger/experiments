import getFib from './fib-o-of-1';

describe('...', () => {
  it('...', () => {
    expect(getFib(1)).toBe(1);
  });
  it('...', () => {
    expect(getFib(0)).toBe(0);
  });

  it('...', () => {
    expect(getFib(2)).toBe(1);
  });

  it('...', () => {
    expect(getFib(3)).toBe(2);
  });

  it('...', () => {
    expect(getFib(10)).toBe(55);
  });

  it('...', () => {
    expect(getFib(15)).toBe(610);
  });

  it('...', () => {
    expect(getFib(25)).toBe(75025);
  });

  it('...', () => {
    expect(getFib(50)).toBe(12586269025);
  });

  it('...', () => {
    expect(getFib(60)).toBe(1548008755920);
  });

  it('...', () => {
    expect(getFib(70)).toBe(190392490709135);
  });
});
