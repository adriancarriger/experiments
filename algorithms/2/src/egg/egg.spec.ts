import { eggBruteForce, egg } from './egg';

describe('...', () => {
  it('...', () => {
    expect(eggBruteForce(100).drops).toBe(14);
  });

  it('...', () => {
    expect(egg(100)).toBe(14);
  });
});
